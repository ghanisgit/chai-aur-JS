"use strict"  // used declare all js is newer version

// alert(3-3)  // can not use this 'cause we are using node.js not browser

let age=null
console.log(typeof age);


// Primitive DT
// number => 0 to 2^53(approx)
// bitInt => greater than 2^53 values
// string => "" '' `` characters,names
// boolean => true/false
// undefined => which is not yet defined(declared but not initialised)
// null => empty,also standalone value, null is an object
// symbol => unique

// Non-primitive DT
// object